/* Babel plugins not only transpile the code but also add the polyfills 
    to the output code. Polyfills will be added only if the target browser doesn't 
    support the feature already. */

const promise = new Promise();
